# Governance-managed Trivy ignore list
# Review quarterly — remove entries when upstream fixes are released
#
# Format: one CVE/ID per line
# Lines starting with # are comments

# CVE-2026-4539 — Low severity (CVSS 3.3) ReDoS in pygments AdlLexer
# Transitive dependency: rich -> pygments
# Affected: pygments <= 2.19.2 — no fix released to PyPI
# Fix merged upstream: pygments/pygments#3064 (2026-03-25)
# Pygments maintainers dispute this as a security issue per their security policy
# Impact: Zero — AdlLexer handles openEHR archetype definitions, unused in this context
# Expiry: 2026-05-31 — re-evaluate after pygments releases fix
# Ref: https://nvd.nist.gov/vuln/detail/CVE-2026-4539
CVE-2026-4539

# CVE-2026-35209 — High severity prototype pollution in defu via __proto__ key
# Transitive dependency: docs-theme -> defu 6.1.4
# Fixed in: defu 6.1.5
# Impact: Low — defu is used for config merging, not user-supplied input
# Expiry: 2026-06-30 — re-evaluate when upstream updates defu
# Ref: https://nvd.nist.gov/vuln/detail/CVE-2026-35209
CVE-2026-35209

# CVE-2026-39363 — Vite dev server arbitrary file read via WebSocket
# Transitive dependency: docs-theme -> vite 7.3.1
# Fixed in: vite 7.3.2
# Impact: Zero — vite dev server is not exposed in production
# Expiry: 2026-06-30 — re-evaluate when upstream updates vite
# Ref: https://nvd.nist.gov/vuln/detail/CVE-2026-39363
CVE-2026-39363

# CVE-2026-39364 — Vite server.fs.deny bypass with queries
# Transitive dependency: docs-theme -> vite 7.3.1
# Fixed in: vite 7.3.2
# Impact: Zero — vite dev server is not exposed in production
# Expiry: 2026-06-30 — re-evaluate when upstream updates vite
# Ref: https://nvd.nist.gov/vuln/detail/CVE-2026-39364
CVE-2026-39364

# CVE-2026-39365 — Medium severity path traversal in vite optimized deps .map handling
# Transitive dependency: docs-theme -> vite 7.3.1
# Fixed in: vite 7.3.2
# Impact: Zero — vite dev server is not exposed in production
# Expiry: 2026-06-30 — re-evaluate when upstream updates vite
# Ref: https://nvd.nist.gov/vuln/detail/CVE-2026-39365
CVE-2026-39365

# DS-0026 — Low severity Dockerfile misconfiguration: missing HEALTHCHECK
# Build containers are ephemeral and not long-running services
# HEALTHCHECK is unnecessary for CI/build images
# Ref: https://avd.aquasec.com/misconfig/ds-0026
DS-0026
